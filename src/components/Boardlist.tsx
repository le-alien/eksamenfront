import React, { useState } from "react";
import "../index.css"

export const Boardlist: React.FC = () => {
    const [] = useState(null);

    

    return (
        <div>

        </div>
    );
}

export default Boardlist;